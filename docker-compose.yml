version: "3"

services:
  telegram-bot:
    image: ghcr.io/danila-initiative/template-bot:${GITHUB_SHA:-latest}
    restart: always
    command: python3 /app/code/user_bot.py
    container_name: telegram_bot_template
    volumes:
      - ./data:/app/data
    env_file:
      - .env
